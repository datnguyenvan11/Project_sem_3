@model IEnumerable<Project_sem_3.Models.HouseInsurance>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<!-- Custom styles for this page -->
<link href="/Assets/admin/vendor/datatables/dataTables.bootstrap4.min.css" rel="stylesheet">
<link href="/Areas/Admin/Assets/css/custom.css" rel="stylesheet">
<!-- Main Content -->
<div id="content">
    <!-- Begin Page Content -->
    <div class="container-fluid">

        <!-- Page Heading -->
        @*<h1 class="h3 mb-2 text-gray-800">Contracts</h1>
            <p class="mb-4"> All contracts of customers about insurances .</p>*@

        <!-- DataTales Example -->
        <div class="card shadow mb-4">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary">Information Contract</h6>
            </div>
            <div class="card-body">
                <div class="table-responsive container">
                    @foreach (var item in Model)
                    {
                        <input type="hidden" name="name" value="@item.ContractId" id="contract_id" />

                        @Html.AntiForgeryToken()

                        <div class="row form-group">
                            <b>Insurance Packages : </b>
                            <div>@item.InsurancePackage.Name</div>
                        </div>
                        <hr />
                        <div class="row">
                            <b>House Type : </b>
                            <div>@item.HouseType</div>
                        </div>
                        <hr />

                        <div class="row">
                            <b>DurationHouse : </b>
                            <div>@item.DurationHouse</div>
                        </div>
                        <hr />

                        <div class="row">
                            <b>HouseOwner : </b>
                            <div>@item.HouseOwner</div>
                        </div>
                        <hr />

                        <div class="row">
                            <b>HouserAddress : </b>
                            <div>@item.HouserAddress</div>
                        </div>
                        <hr />

                        <div class="row">
                            <b>Quantity : </b>
                            <div>@item.Quantity</div>
                        </div>
                        <hr />

                        <div class="row">
                            <b>UnitPrice : </b>
                            <div>@item.UnitPrice</div>
                        </div>
                        <hr />

                        <div class="row">
                           
                            <div>
                                <div class="row mt-3">
                                    <div class="col-3 mt-2">
                                        <div class="form-group mr-3">
                                            <select class="form-control mr-2" id="select-action" style="width: auto;">
                                                <option value="6">--Chọn thao tác--</option>
                                                <option value="0">Confirmed</option>
                                                <option value="-1">Deleted</option>
                                                <option value="2">Pay</option>
                                                <option value="3">Done</option>
                                                <option value="4">All</option>

                                            </select>
                                        </div>
                                    </div>
                                    <div class="col-9" style="margin-top: 8px;">
                                        <button type="button" id="action_contract" class="btn btn-success" style="float: right;">Confirm</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
            @*<div class="card-body">
                <div class="table-responsive">
                    <div id="dataTable_wrapper" class="dataTables_wrapper dt-bootstrap4">
                        <div class="row"><div class="col-sm-12 col-md-6"><div class="dataTables_length" id="dataTable_length"><label>Show <select name="dataTable_length" aria-controls="dataTable" class="custom-select custom-select-sm form-control form-control-sm"><option value="10">10</option><option value="25">25</option><option value="50">50</option><option value="100">100</option></select> entries</label></div></div><div class="col-sm-12 col-md-6"><div id="dataTable_filter" class="dataTables_filter"><label>Search:<input type="search" class="form-control form-control-sm" placeholder="" aria-controls="dataTable"></label></div></div></div>
                        <div class="row">
                            <div class="col-sm-12">
                                <table class="table table-bordered dataTable" id="dataTable" width="100%" cellspacing="0" role="grid" aria-describedby="dataTable_info" style="width: 100%;">
                                    <thead>

                                        <tr>
                                            <th rowspan="1" colspan="1">Insurance Packages</th>
                                            <th rowspan="1" colspan="1">House Type</th>
                                            <th rowspan="1" colspan="1">DurationHouse</th>
                                            <th rowspan="1" colspan="1">HouseOwner</th>
                                            <th rowspan="1" colspan="1">HouserAddress</th>
                                            <th rowspan="1" colspan="1">Quantity</th>
                                            <th rowspan="1" colspan="1">UnitPrice</th>
                                            <th rowspan="1" colspan="1">Action</th>
                                        </tr>
                                    </thead>

                                    <tbody>

                                        @foreach (var item in Model)
                                        {
                                            <input type="hidden" name="name" value="@item.ContractId" id="contract_id" />
                                            @Html.AntiForgeryToken()

                                            <tr role="row" class="odd">

                                                <td>@item.InsurancePackage.Name</td>
                                                <td>@item.HouseType</td>
                                                <td>@item.DurationHouse</td>
                                                <td>@item.HouseOwner</td>
                                                <td>@item.HouserAddress</td>
                                                <td>@item.Quantity</td>
                                                <td>$@item.UnitPrice</td>
                                                <td>
                                                    <div class="card-footer">
                                                        <div class="row mt-3">
                                                            <div class="col-3 mt-2">
                                                                <div class="form-group mr-3">
                                                                    <select class="form-control mr-2" id="select-action" style="width: 100px;">
                                                                        <option value="6">--Chọn thao tác--</option>
                                                                        <option value="0">Confirmed</option>
                                                                        <option value="-1">Deleted</option>
                                                                        <option value="2">Pay</option>
                                                                        <option value="3">Done</option>
                                                                        <option value="4">All</option>

                                                                    </select>
                                                                </div>
                                                            </div>
                                                            <div class="col-9" style="margin-top: 8px;">
                                                                <button type="button" id="action_contract" class="btn btn-success" style="float: right;">Confirm</button>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                </div>
            </div>
        </div>*@

        </div>
    <!-- /.container-fluid -->
</div>
<!-- End of Main Content -->
@section scripts
{
    <script>
        

        $('#action_contract').click(function () {
            var selectedID = $('#contract_id').val();
            var selectedIDs = new Array();
            selectedIDs.push(selectedID);
            console.log(selectedIDs);
            var action = $('#select-action').val();
            if (action == 6) {
                alert('Please select the action you want to perform!');
                return;
            }

            if (selectedIDs.length <= 0) {
                alert('You have not selected a contract!');
                return;
            }

            if (action == -1) {
                if (confirm("Do you want to Delete?")) {
                    changeStatusPTTime(selectedIDs, action);
                }
            }
            if (action == 0) {
                if (confirm("Do you want to status Confirmed?")) {
                    changeStatusPTTime(selectedIDs, action);
                }
            }
            if (action == 1) {
                if (confirm("Do you want to status Pending?")) {
                    changeStatusPTTime(selectedIDs, action);
                }
            }
            if (action == 2) {
                if (confirm("Do you want to change status pay?")) {
                    changeStatusPTTime(selectedIDs, action);
                }
            }
            if (action == 3) {
                if (confirm("Do you want to change status done?")) {
                    changeStatusPTTime(selectedIDs, action);
                };
            }

        });

        function changeStatusPTTime(selectedIDs, action) {
            $.ajax({
                url: "/Admin/Contracts/ChangeStatus",
                type: "POST",
                dataType: "json",
                data: {
                    __RequestVerificationToken: $('input[name=__RequestVerificationToken]').val(),
                    selectedIDs,
                    action
                },
                success: function (res) {
                    if (!res == false) {
                        window.location.reload();
                    }
                },
                error: function () {
                    alert('error');
                }
            });
        }
    </script>

}

